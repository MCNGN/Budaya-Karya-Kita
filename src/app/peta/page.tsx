"use client";

import { useEffect } from "react";
// import * as d3 from "d3";
import Header from "../components/Header";

export default function Map() {
  useEffect(() => {

    // d3.json("/indonesia-prov.geojson").then((d) => {
    //   return d;
    // }).then((worldData) => {
    //   console.log(worldData)
    // })
  });

  return (
    <div>
      <Header />
    </div>
  );
}
